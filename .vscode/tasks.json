{
  "tasks": [
    {
      "label": "JupyText Sync",
      "type": "shell",
      "command": "${command:python.interpreterPath} -m jupytext --sync ${file}",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "never",
      }
    }
  ]
}